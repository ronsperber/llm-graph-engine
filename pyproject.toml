[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "llm-graph-engine"
version = "0.1.0"
dependencies = []

[tool.setuptools]
package-dir = {"" = "src"}